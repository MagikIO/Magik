# moon project configuration for @magik_io/session-redis
$schema: 'https://moonrepo.dev/schemas/project.json'

type: 'library'
language: 'typescript'
tags: ['session', 'redis', 'store']

dependsOn:
  - 'preset-session'
