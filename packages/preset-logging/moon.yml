# moon project configuration for @magik_io/preset-logging
$schema: 'https://moonrepo.dev/schemas/project.json'

tags: ['preset', 'logging', 'middleware']

dependsOn:
  - 'magik'

tasks:
  extends: 'global'
