# moon project configuration for @magik_io/preset-parser
$schema: 'https://moonrepo.dev/schemas/project.json'

type: 'library'
language: 'typescript'
tags: ['preset', 'parser', 'middleware']

dependsOn:
  - 'magik'
