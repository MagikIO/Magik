# moon project configuration for @magik_io/upload-local
$schema: 'https://moonrepo.dev/schemas/project.json'

type: 'library'
language: 'typescript'
tags: ['upload', 'local', 'storage']

dependsOn:
  - 'magik'
