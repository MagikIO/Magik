# moon project configuration for @magik_io/preset-compression
$schema: 'https://moonrepo.dev/schemas/project.json'

tags: ['preset', 'compression', 'middleware']

dependsOn:
  - 'magik'

tasks:
  extends: 'global'
